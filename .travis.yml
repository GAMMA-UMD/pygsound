os: osx
language: python
before_install:
  - brew install fftw
  - brew install cmake
# command to install dependencies
install:
  - python3 setup.py develop
# command to run tests
script:
  - python3 setup.py test
